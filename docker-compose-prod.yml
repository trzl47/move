version: '3'
services:
  web:
    image: trzl/portfolio:move-web_v0.0.1
    container_name: MOVE-web
    restart: unless-stopped
    expose:
      - "80"
      - "443"
    environment:
      VIRTUAL_HOST: move.tbrownkcdev.com
      LETSENCRYPT_HOST: move.tbrownkcdev.com
      LETSENCRYPT_EMAIL: brownty47@gmail.com
networks:
  default:
    external:
      name: webproxy
